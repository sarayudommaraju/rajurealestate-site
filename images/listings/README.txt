Drop real photos here, one folder per listing id (matches data/listings.json).
Name files exactly as listed in that listing's images[] array, e.g. 1.jpg, 2.jpg.
Missing files auto-fall back to /images/placeholder.svg, so partial uploads are safe.
Recommended: JPG/WebP, ~1600x1200, under 400 KB each.
